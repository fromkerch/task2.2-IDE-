public class Main {
    public static void main(String[] args) {
         float regularBonus = (float) 0.3;
         float specialBonus = (float) 0.6;
         float totalBonus = regularBonus + specialBonus;
        System.out.println(totalBonus);
    }
}